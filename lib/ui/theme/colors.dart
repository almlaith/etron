import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Main Brand Colors
  static const Color primary = Color(0xFF4CB050);
  static const Color secondary = Color(0xFF5267B0);
  static const Color tertiary = Color(0xFF7057A5);

  // Feedback Colors
  static const Color success = primary;
  static const Color black = Color(0xFF000000);
  static const Color warning = Color(0xFFFFB300);
  static const Color error = Color(0xFFF44336);
  static const Color darkTeal = Color(0xFF0D2627);

  // Background & Surface
  static const Color backgroundLight = Color(0xFFF5F5F5);
  static const Color backgroundDark = Color(0xFF1C1C1E);
  static const Color surface = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF888888);
  static const Color grayLight = Color(0xA69E9E9E);

  // Dividers & Borders
  static const Color divider = Color(0xFFE0E0E0);

  // Other Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color transparent = Color(0x00000000);
}
